import PlaceholderPage from "@/components/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      note="Settings (currency symbol and more) arrive in a later feature."
    />
  );
}
